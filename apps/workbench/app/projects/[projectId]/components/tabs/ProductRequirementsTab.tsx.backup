/**
 * ProductRequirementsTab Component
 *
 * Tab 1: Product Requirements (PRD Sections)
 *
 * Purpose: Lock the foundation quickly
 * - Lists PRD sections (Personas, Key Features, Happy Path, Constraints)
 * - Each section has status badges
 * - Consultant can confirm or mark for client confirmation
 *
 * This is a placeholder - will be implemented in Phase 3
 */

'use client'

import React from 'react'
import { EmptyState } from '@/components/ui'
import { FileText } from 'lucide-react'

interface ProductRequirementsTabProps {
  projectId: string
}

export function ProductRequirementsTab({ projectId }: ProductRequirementsTabProps) {
  return (
    <div className="p-6">
      <EmptyState
        icon={<FileText className="h-16 w-16" />}
        title="Product Requirements Tab"
        description="This tab will show PRD sections with status tracking. Implementation coming in Phase 3."
      />
    </div>
  )
}
